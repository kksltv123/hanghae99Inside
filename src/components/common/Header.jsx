import React from 'react';
import Layout from './Layout';

const Header = () => {
    return (
        <div>
            <Layout>
                
            </Layout>
        </div>
    );
};

export default Header;